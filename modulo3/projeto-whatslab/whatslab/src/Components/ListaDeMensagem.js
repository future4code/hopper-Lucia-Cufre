/* import React from "react";

class ListaDeMensagem extends React.Component {
  state = {
    mensagens: [
      {
        usuario: "",
        conteudo: "",
      },
    ],
  }; */

  /* enviaMensagem = () => {

const novaMensagen = {
usuario: this.state.mensagens.usuario,
conteudo: this.state.mensagens.conteudo
};

const novasMensagens = [...this.state.mensagens, novaMensagen]

this.setState({mensagens:novasMensagens})
this.setState({usuario: "", conteudo:""})

} */
/* 
  render() {
    const listaDeMensagens = this.state.mensagens.map((mensagen, index) => {
      return (
        <p>
          '${mensagen.usuario}: ${mensagen.conteudo}'
        </p>
      );
    });

    return <div>{listaDeMensagens}</div>;
  }
}

export default ListaDeMensagem; */
